# 构建上下文排除项。
# 镜像直接用宿主机打包好的 jar（见 Dockerfile），所以 target/ 下只有 jar 需要进上下文，
# 其余（classes、测试报告、临时文件）都是白传给守护进程的负担。

target/*
!target/*.jar

# 本地环境变量绝不能进镜像：容器里的配置靠 --env-file / -e 在运行时注入
.env
.env.local
.env.*.local

.git/
.gitignore
.idea/
*.iml
.vscode/
.claude/

HELP.md
README.md
