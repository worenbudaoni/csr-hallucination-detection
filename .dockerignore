# 构建上下文排除项。
# 构建在容器内的多阶段流程里从源码编译，所以本地产物和本地配置都不该进上下文。

target/

# 本地环境变量绝不能进镜像：容器里的配置靠 --env-file / -e 在运行时注入
.env
.env.local
.env.*.local

.git/
.gitignore
.idea/
*.iml
.vscode/
.claude/

HELP.md
README.md
